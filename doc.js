// Commit : Added JSX and Babel setup

// In this commit I have done setup for this babel support.
// Installed node modules. cmd => `npm init -y`
// Installed babel for react cmd => `npm install @babel/cli@7 babel-preset-react-app@10`
// Move `index.js` with JSX syntax to `src` folder.
// Run `npx babel --watch src --out-dir . --presets babel-preset-react-app/prod` to watch `src` folder 
// and parse it to plain and browser compatible javascript.